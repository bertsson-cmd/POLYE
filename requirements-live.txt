# Extra dependencies needed only for live deployment (run_forever.py /
# live.py / control_server.py) -- NOT needed for the paper-trading GitHub
# Actions workflow, which only ever installs requirements.txt.
#
# polymarket-client (imports as `polymarket`, GitHub Polymarket/py-sdk) --
# Polymarket's official unified SDK, used instead of py-clob-client-v2.
# py-clob-client-v2 is fully retired for this account: its deposit-wallet
# (signature_type=3/POLY_1271) order placement is confirmed broken
# (Polymarket/py-clob-client-v2 issues #64, #70, #75, #105, all open), and
# this account's exchange started hard-rejecting signature_type=1
# (POLY_PROXY) orders outright ("maker address not allowed, please use the
# deposit wallet flow"). polymarket-client's AsyncSecureClient was verified
# end-to-end for this exact account via scripts/test_deposit_wallet.py
# before live.py was switched over -- see LIVE.md. Pinned to the version
# verified against source during that switch -- bump deliberately,
# re-checking polyedge/live.py's assumptions against the new version's
# actual API surface, not blindly.
-r requirements.txt
polymarket-client==0.2.0
flask
